chapter: "1.3"
criteria:
  - id: extraction_tool_schema_shape
    description: "extraction_tool returns {name, description, input_schema} where input_schema is a JSON-schema object with the given properties and required list"
    weight: 25
    check: code_review
  - id: forces_tool_choice
    description: "force_tool_choice returns {\"type\": \"tool\", \"name\": name} to force the specific tool"
    weight: 15
    check: code_review
  - id: reads_tool_use_input
    description: "parse_tool_result returns the .input of the tool_use block whose .name matches tool_name"
    weight: 25
    check: code_review
  - id: no_free_text_json_parsing
    description: "Does NOT parse JSON from the model's free-text output; parse_tool_result raises ValueError when no matching tool_use block is present"
    weight: 20
    check: anti_pattern
  - id: tests_pass
    description: "All tests in test_structured_output.py pass"
    weight: 15
    check: bash_execution
    command: "pytest test_structured_output.py -v"
